import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#FF7043',
    },
    secondary: {
      main: '#cfcfcf',
    },
    text: {
      primary: '#cfcfcf',
      secondary: 'FF7043'
    },

  },

});
